# ai_back_test_all_stock.py 改造方案与规划

## 目标

把当前的 `ai_back_test_all_stock.py` 从“能跑的全市场回测脚本”改造成一个更适合指导实盘研究的“统一回测引擎”。

核心目标不是追求更好看的收益率，而是提高回测可信度、可复现性、板块对比公平性和后续实盘落地能力。

## 当前脚本定位

当前脚本已经具备几个重要基础：

- 支持 `ALL`、`MAIN`、`CYB`、`KCB` 板块开关。
- 有训练集参数寻优和样本外测试。
- 考虑了手续费、最低佣金、印花税和滑点。
- 有大盘红绿灯过滤、个股打分、止损止盈和持仓天数控制。
- 有数据缓存机制，避免每次重复下载全部历史行情。

但如果要用于实盘决策研究，仍然需要先解决几个会影响结论可信度的问题。

## 改造原则

1. 一个统一引擎，多套板块配置。
2. 创业板、科创板、主板要单独评估，但不复制成多个大脚本。
3. 先修正回测口径和数据样本，再优化策略参数。
4. 每一步改造都要能单独运行和验证。
5. 实盘约束优先于收益曲线美观。

## 一、板块配置统一化

### 改什么

把当前散落在 `CONFIG` 和股票池过滤逻辑里的板块规则，整理成 `BOARD_PROFILES`：

```python
BOARD_PROFILES = {
    "MAIN": {
        "name": "主板",
        "prefixes": (...),
        "price_limit": 0.10,
        "max_position_per_stock": 0.20,
        "max_holdings": 5,
    },
    "CYB": {
        "name": "创业板",
        "prefixes": (...),
        "price_limit": 0.20,
        "max_position_per_stock": 0.15,
        "max_holdings": 5,
    },
    "KCB": {
        "name": "科创板",
        "prefixes": (...),
        "price_limit": 0.20,
        "max_position_per_stock": 0.12,
        "max_holdings": 5,
    },
    "ALL": {
        "name": "全A混合",
        "prefixes": (...),
        "price_limit": None,
        "max_position_per_stock": 0.15,
        "max_holdings": 8,
    },
}
```

### 为什么改

主板、创业板、科创板的交易制度、波动率、流动性和账户权限都不同。如果全部共用一套隐含规则，回测结果容易混在一起，最后很难判断策略到底适合哪个板块。

不建议复制成多个脚本，因为复制后核心逻辑容易漂移。例如主板脚本改了止损，创业板脚本忘了改，对比结果就不再公平。

### 怎么改

- 保留 `ai_back_test_all_stock.py` 作为唯一核心引擎。
- 新增 `BOARD_PROFILES`。
- `CONFIG["TARGET_BOARD"]` 只负责选择当前板块。
- 股票代码过滤、缓存目录、最大持仓数、单票仓位等都从 profile 读取。

## 二、股票池构建改造

### 改什么

当前逻辑是从 A 股名录中过滤代码前缀，然后截取前 400 只：

```python
filtered_pool = dict(pool_items[:400])
```

这需要改成“可交易股票池过滤”：

- 排除 ST、退市、风险警示股票。
- 排除上市时间不足的股票，例如不足 120 或 250 个交易日。
- 排除历史数据不足的股票。
- 增加流动性过滤，例如最近 20 日成交额均值不低于某个阈值。
- 分板块模式下分别构建股票池。
- `ALL` 模式不要简单取前 400 只。

### 为什么改

如果接口返回顺序按代码排列，取前 400 只会造成样本偏差。比如可能偏向沪市主板或某些代码段，而不是代表真实市场。

实盘真正能交易的不是“所有股票”，而是“可交易、流动性足够、数据足够完整”的股票。股票池质量会直接影响回测结论。

### 怎么改

第一阶段先采用保守方案：

- 不再固定取前 400 只。
- 设置 `MAX_STOCKS_PER_BOARD` 作为可选调试限制，默认不限制或显式标注为抽样模式。
- 先拉取基础名录，只做代码和名称过滤。
- 在 `prepare_stock_data()` 后根据历史数据长度、价格和成交量过滤。

后续可进一步增强：

- 通过成交额排序选取高流动性股票。
- 支持分层抽样，避免 `ALL` 模式被某个板块主导。
- 从每天保存的快照构建自己的 point-in-time 股票池。

## 三、交易日期轴改造

### 改什么

当前使用所有股票日期的交集：

```python
all_dates = sorted(list(set.intersection(*[set(df.index) for df in market_data.values()])))
```

需要改成用统一交易日历推进回测：

- 优先使用基准指数日期，例如沪深300或中证全指。
- 每个交易日到了之后，再判断个股当天是否有行情数据。
- 个股缺失数据时跳过该个股，不删除整个交易日。

### 为什么改

“所有股票日期交集”会把很多正常交易日删掉。只要某只股票停牌、上市较晚或数据缺失，那些日期就可能被排除。

这会导致回测不是在真实交易日上运行，而是在一个被过度压缩的日期集合上运行，结论可能失真。

### 怎么改

- 新增 `get_trading_calendar(benchmark_data, start_date, end_date)`。
- 回测主循环使用交易日历：

```python
period_dates = get_trading_calendar(benchmark_data, start_date, end_date)
```

- 买卖、估值、扫描时分别检查：

```python
if current_date not in market_data[symbol].index:
    continue
```

- 如果持仓股票当天没数据，估值时使用最近一个可用收盘价，或先采用保守方案：不更新估值并记录缺失。

## 四、成交可行性与涨跌停约束

### 改什么

当前只检查开盘高开是否超过 4%：

```python
if (ideal_price - prev_close) / prev_close > 0.04:
    continue
```

需要增加更真实的成交约束：

- 涨停附近不追买。
- 跌停附近不能假设顺利卖出。
- 停牌或无成交量时不能交易。
- 开盘跳空过大时不买。
- 创业板、科创板使用 20% 涨跌幅规则，主板使用 10%。

### 为什么改

A 股实盘里，“信号出现”和“能成交”是两回事。回测如果默认所有买卖都能按开盘价成交，会高估策略表现。

尤其是右侧突破策略，容易在强势票上遇到高开、涨停、流动性不足等问题。

### 怎么改

- 新增 `can_buy(symbol, row, prev_close, board_profile)`。
- 新增 `can_sell(symbol, row, prev_close, board_profile)`。
- 根据不同板块的涨跌幅限制判断是否接近涨停/跌停。
- 成交量为 0 或缺失时禁止交易。
- 保留滑点，但对高开或极端行情提高滑点假设。

## 五、策略打分模块化

### 改什么

把当前买入打分逻辑从回测主循环中抽出：

```python
score = score_stock(today_k, recent_5, params)
```

同时让实时扫描脚本复用同一套评分函数。

### 为什么改

现在 `ai_back_test_all_stock.py` 和 `today_buy_signal_hs_300.py` 各自写了一份相似打分逻辑。以后只要其中一个改了，另一个没改，回测和实盘扫描就会不一致。

实盘最怕“回测是一套，执行是一套”。

### 怎么改

- 新增 `score_stock(df, current_date, params)`。
- 返回分数和命中的评分项。
- 回测和实盘扫描都调用同一函数。
- 输出文件中增加评分明细，便于复盘。

## 六、卖出和持仓管理模块化

### 改什么

把卖出判断从主循环中抽出：

```python
sell_reason = evaluate_exit(pos, today_k, params)
```

### 为什么改

实盘里真正决定亏损可控性的不是买入信号，而是持仓管理。卖出逻辑独立后，才能做单独验证和复盘。

### 怎么改

- 新增 `evaluate_exit(position, row, params)`。
- 保留现有规则：
  - 绝对止损
  - 破位防守
  - 回撤止盈
  - 亢奋止盈
  - 沉没止损
- 输出卖出触发时的关键指标，例如收益率、RSI、是否跌破 MA20。

## 七、绩效统计与报告修正

### 改什么

当前盲测报告里使用交易明细的单笔净利汇总计算最终资金：

```python
total_net_profit = trade_df["单笔净利"].sum()
final_total_capital = initial_capital + total_net_profit
```

需要改成以每日权益曲线的最后一天为准。

### 为什么改

如果期末还有未平仓仓位，已实现盈亏不等于真实总权益。用交易明细求和会忽略浮盈浮亏，容易误导。

### 怎么改

- `execute_single_backtest()` 返回 `权益曲线`。
- 期末权益使用：

```python
final_equity = equity_df["总权益"].iloc[-1]
```

- 报告同时展示：
  - 期初资金
  - 期末总权益
  - 已实现盈亏
  - 未实现盈亏
  - 总收益
  - 年化收益
  - 最大回撤
  - Calmar
  - Sharpe
  - 交易次数
  - 胜率
  - 盈亏比
  - 平均持仓天数

## 八、滚动验证

### 改什么

当前是单次训练集和单次测试集：

- 训练：`TRAIN_START` 到 `TRAIN_END`
- 测试：`TEST_START` 到 `TEST_END`

需要增加滚动验证模式：

- 例如 3 年训练 + 1 年测试。
- 每年向前滚动一次。
- 分别统计每个测试窗口表现。

### 为什么改

一次训练、一次测试很容易碰到偶然行情。实盘更关心策略是否在多个年份、多个市场阶段都能活下来。

### 怎么改

第二阶段再做，先不急着改第一版：

- 新增 `WALK_FORWARD_WINDOWS`。
- 每个窗口独立寻优。
- 输出年度/窗口表现表。
- 选择不是收益最高，而是多窗口最稳定的参数。

## 九、推荐实施顺序

### 第 1 阶段：先修回测口径

目标：让回测日期和股票池更接近真实实盘环境。

- 引入 `BOARD_PROFILES`。
- 修复 `ALL` 模式简单截取前 400 只的问题。
- 改交易日期轴为基准指数交易日历。
- `execute_single_backtest()` 返回权益曲线。
- 修正最终资金统计口径。

验收标准：

- `MAIN`、`CYB`、`KCB`、`ALL` 都能单独运行。
- 每个模式输出独立交易记录和绩效摘要。
- 回测日志明确标注股票池构建方式。

### 第 2 阶段：加入实盘成交约束

目标：减少回测高估。

- 加停牌、无成交量过滤。
- 加涨跌停附近不可买卖判断。
- 按板块区分 10% 和 20% 涨跌幅。
- 增加高开不追买规则配置化。

验收标准：

- 每笔被跳过的买卖有原因统计。
- 报告中展示信号数、实际成交数、跳过数。

### 第 3 阶段：模块化策略逻辑

目标：保证回测和实盘扫描使用同一套规则。

- 抽出 `score_stock()`。
- 抽出 `evaluate_exit()`。
- 输出评分明细。
- 让实时选股脚本复用评分函数。

验收标准：

- 同一日期、同一股票，回测和实时扫描评分一致。
- 后续调整评分规则只需要改一个地方。

### 第 4 阶段：滚动验证和参数稳定性

目标：判断策略是否只是某段行情有效。

- 增加 walk-forward 回测。
- 输出多窗口绩效。
- 增加参数敏感性分析。

验收标准：

- 能看到每个年份或窗口的独立表现。
- 最优参数不是只在单一窗口表现突出。

## 十、短期不建议做的事

这些事情可以以后做，但不建议现在优先做：

- 上来就扩大参数网格。
- 直接加机器学习模型。
- 只按收益率挑最优参数。
- 同时大改回测、实盘扫描和所有参数。
- 为主板、创业板、科创板复制三份完整脚本。

## 十一、下一步建议

优先从第 1 阶段开始：

1. 新增 `BOARD_PROFILES`。
2. 改股票池构建，不再默认取前 400 只。
3. 用基准指数日期作为交易日历。
4. 修正最终权益统计。

完成这一步后，再分别跑 `MAIN`、`CYB`、`KCB`，比较三个板块的策略表现。确认哪个板块更适合当前策略后，再考虑是否做 `ALL` 混合模式。
